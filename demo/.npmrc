# Copy the file: dependency instead of symlinking it, so "react" resolves to
# THIS app's node_modules — a symlink would resolve to the package's own dev
# copy of React and crash with two React instances.
install-links=true
